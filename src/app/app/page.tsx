import TProtocols from '@/components/templates/Protocols';
import React, { FC } from 'react';

// import { Container } from './styles';

const app: FC = () => {
  return <TProtocols />;
}

export default app;